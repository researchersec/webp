{
  "name": "demoparser-web",
  "version": "1.0.0",
  "description": "A web application to parse .dem files using demoparser2 and Webpack for GitHub Pages",
  "main": "src/index.js",
  "scripts": {
    "build": "webpack --mode production",   // Build your app for production
    "start": "webpack serve --mode development --open",  // Start local dev server
    "deploy": "gh-pages -d dist"  // Deploy manually if needed (using gh-pages CLI)
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "demoparser2": "^1.0.0"  // Assuming this is the correct version for demoparser2
  },
  "devDependencies": {
    "@babel/core": "^7.21.0",
    "@babel/preset-env": "^7.21.0",
    "babel-loader": "^9.1.2",
    "path-browserify": "^1.0.1",
    "stream-browserify": "^3.0.0",
    "buffer": "^6.0.3",
    "process": "^0.11.10",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.4",
    "webpack-dev-server": "^4.15.0",
    "gh-pages": "^5.0.0"  // If you want to deploy manually with npm run deploy
  },
  "browserslist": [
    "defaults"
  ]
}
